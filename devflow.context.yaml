# Devflow workflow context directory for this repository.
# Skills and contracts live under this folder (default: artifacts/).
#
# Override order:
#   1. DEVFLOW_CONTEXT_DIR environment variable
#   2. This file (context_dir)
#   3. Package default (artifacts)
context_dir: artifacts
