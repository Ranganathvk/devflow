# Project state — <PROJECT_NAME>

> Durable working state for this repository. `SPEC.md` changes rarely; this
> file tracks current progress.

## Current focus

One line naming the active feature or stage.

## Active workflow stage

Pick one: `spec-alignment` / `system-shape` / `design` / `tdd` / `tasksplit` /
`implement` / `review` / `learn`.

## Recent decisions

- `<YYYY-MM-DD>` — <decision> — <outcome>.

## Open blockers

- `[BLOCKER]` <what is blocked and what unblocks it>.

## Pending tasks

| Task ID | State | Notes |
|---------|-------|-------|
| `<FEATURE>:C1` | `pending` / `in-progress` / `done` | … |

## Last implementation

What landed, verification performed, and what comes next.

## Notes for the next session

Facts needed to continue without re-reading chat history.
