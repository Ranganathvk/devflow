---
name: feature-design
description: >-
  DEPRECATED — use /design instead. Produces the same _DESIGN.md and .contract.yaml artifacts.
---

# /feature-design — Deprecated

**Use `/design <FEATURE>`** — produces `AI_CONTEXT/<FEATURE>_DESIGN.md` and `<FEATURE>.contract.yaml` with delivery classification.

Do not extend this skill.
