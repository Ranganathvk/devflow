---
name: feature-db
description: >-
  DEPRECATED — use /design instead. DB planning runs in the db stage when needs_db is true.
---

# /feature-db — Deprecated

**Use `/design <FEATURE>`** — when `delivery.needs_db: true`, the **db** stage emits `_DB.md` and `_DB.contract.yaml`.

Do not extend this skill.
