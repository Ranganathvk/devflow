---
name: feature-api
description: >-
  DEPRECATED — use /design instead. API planning runs in the api stage when needs_api is true.
---

# /feature-api — Deprecated

**Use `/design <FEATURE>`** — when `delivery.needs_api: true`, the **api** stage emits `_API.md` and `_API.contract.yaml`.

Do not extend this skill.
